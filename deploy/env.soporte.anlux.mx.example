# Copiar a .env en el servidor (soporte.anlux.mx)
# Ajustar DB_* con los valores de cPanel (usuario MySQL de anlux_os, etc.)

APP_NAME=Anlux
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://soporte.anlux.mx

DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=anlux_os
DB_USERNAME=joses16_admin
DB_PASSWORD=CAMBIAR

SESSION_DRIVER=database
SESSION_DOMAIN=null

QUEUE_CONNECTION=database

ANLUX_LEGACY_APP_URL=https://anlux.mx
ANLUX_SHOW_LEGACY_ESCAPE_HATCH=false
